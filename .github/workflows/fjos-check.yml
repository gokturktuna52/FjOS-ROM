name: FjOS GSI Check

on:
  workflow_dispatch:
  push:

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: FjOS Check
        run: |
          echo "FjOS repository OK"
          echo "FjOS GSI workflow OK"
